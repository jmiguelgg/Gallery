/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Albums from './src/screens/Albums';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Albums);
